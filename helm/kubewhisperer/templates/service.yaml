apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubewhisperer.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kubewhisperer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubewhisperer.selectorLabels" . | nindent 4 }}
