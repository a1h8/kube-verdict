# Web UI
streamlit>=1.35.0

# K8s client — supports server API from 1.16 to latest
kubernetes>=29.0.0

# LangGraph workflow (RCA as a stateful, human-in-the-loop graph)
langgraph>=0.2.14

# Vector search + signal analysis
faiss-cpu>=1.8.0
sentence-transformers>=3.0.0
transformers>=4.40.0     # PatchTST (signals/), sentence-transformers backbone
torch>=2.0.0             # PatchTST training + sentence-transformers

# Deduplication / ranking
scikit-learn>=1.4.0
rank-bm25>=0.2.2

# Utilities
requests>=2.31.0
pyyaml>=6.0.1
python-dotenv>=1.0.0
numpy>=1.26.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Optional: helmfile CLI (external binary, not a Python package)
# Install via: https://github.com/helmfile/helmfile/releases
