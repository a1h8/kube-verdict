[pytest]
testpaths = tests
# --timeout: safety net so a single hung test (e.g. a blocked network call to
# huggingface.co) fails fast instead of stalling the job for ~1h.
addopts = -v --tb=short --timeout=300
asyncio_mode = auto
markers =
    integration: requires Ollama + live cluster — skipped in CI
